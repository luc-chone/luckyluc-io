

// take the full screen width and height
export const screenWidth = window.innerWidth;
export const screenHeight = window.innerHeight;

export const groundHeight = 50;
export const groundY = screenHeight - groundHeight;

